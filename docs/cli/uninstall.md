---
summary: "CLI reference for `gensparx uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `gensparx uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
gensparx uninstall
gensparx uninstall --all --yes
gensparx uninstall --dry-run
```
