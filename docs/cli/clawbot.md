---
summary: "CLI reference for `gensparx clawbot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `gensparx clawbot ...`
  - You need migration guidance to current commands
title: "clawbot"
---

# `gensparx clawbot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `gensparx clawbot qr` (same behavior as [`gensparx qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `gensparx clawbot qr` -> `gensparx qr`
